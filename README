gradience.py -- version 1.1
=========================================================================

Visible in node editor sidebar under 'Item' category when a color ramp node is selected.

Add color stops using 'add' button.  Use 'X' buttons on color stops to remove them.

Adjust parameters or use randomize button.

Use 'ramp' button to send to the node.
